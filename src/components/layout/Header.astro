---
import Search from "@/components/Search.astro";
import ThemeToggle from "@/components/ThemeToggle.astro";
import { siteConfig, menuLinks } from "@/site.config";
import { Icon } from "astro-icon/components";
---

<header
	id="main-header"
	class="fixed left-0 top-0 z-20 flex h-16 w-full items-center overflow-hidden border-b border-color-150/60 bg-bgColor/90 px-4 backdrop-blur-md transition-all duration-200 ease-out md:relative md:border-b-0 md:bg-transparent md:px-0"
>	
	<!-- Background 
			TODO: This approach is not optimal and requires improvements. 
  			- Too many absolutely positioned elements can affect performance. 
  		-->	
	<div class="pointer-events-none absolute left-1/2 top-0 -ml-[50vw] min-h-screen w-screen blur-3xl md:hidden" aria-hidden="true">
		<div class="editorial-gradient-orb absolute top-[-90%] right-[20%] h-full w-[70%] bg-gradient-to-b from-amber-200/70 via-stone-300/30 to-transparent"></div>
		<div class="editorial-gradient-orb absolute top-[-85%] left-[15%] h-full w-[55%] bg-gradient-to-b from-stone-400/40 to-transparent"></div>
	</div>
	
	<div class="w-full justify-between sm:flex-col">
		<div class="flex items-center gap-x-2">
			<a
				aria-label={siteConfig.title}
				aria-current={Astro.url.pathname === "/" ? "page" : false}
				class="group flex h-8 items-center hover:filter-none sm:relative"
				href="/"
			>	
				<!-- Logo -->
				<!--
				<div class="pt-1.5">
					<img 
        src="src/assets/images/DFWSS-2.png" 
        alt={siteConfig.title} 
        class="inline-block h-8 w-8 object-contain drop-shadow-[0px_2.5px_2.5px_rgba(0,0,0,0.35)]"
    />
				</div>
				-->
				<!-- filepath: /Users/singhz/Desktop/dfw-sikh-gun-club-website/DFWSS2/src/components/layout/Header.astro -->
<!-- filepath: /Users/singhz/Desktop/dfw-sikh-gun-club-website/DFWSS2/src/components/layout/Header.astro -->
<div title={siteConfig.title}>
    <img 
        src="/assets/images/DFWSS-2.png" 
        alt={siteConfig.title} 
        class="inline-block h-24 w-24 object-contain drop-shadow-editorial transition-transform duration-200 ease-out group-hover:scale-[1.02]"
    />
</div>
				
				<strong class="'max-[320px]:hidden' z-10 mb-0.5 ms-3 hidden bg-gradient-to-r from-accent-base via-accent-one to-accent-two bg-clip-text font-display text-2xl font-medium tracking-tight text-transparent transition-all duration-200 ease-out group-hover:from-accent-one group-hover:to-accent-two md:block">
    {siteConfig.title}
</strong>
			</a>
			<nav
				aria-label="Main menu"
				class="top-20 mx-auto ml-4 ml-auto hidden flex-col items-end justify-center gap-x-6 rounded-sm bg-bgColor text-sm font-medium tracking-wide text-accent-base shadow-editorial backdrop-blur-md transition-all duration-200 ease-out group-[.menu-open]:z-50 group-[.menu-open]:flex sm:static sm:z-auto sm:flex-row sm:items-center sm:rounded-none sm:bg-transparent sm:shadow-none sm:backdrop-blur-none md:flex"
				id="main-navigation-menu"
			>	
				<!--				
				<a
					aria-current={Astro.url.pathname === "/" ? "page" : false}
					class="flex gap-x-1 h-8 items-center justify-center rounded-lg underline-offset-2 hover:underline"
					href="/"
				>	
					<Icon
						class="size-4 drop-shadow-[0px_1.5px_1.5px_rgba(0,0,0,0.175)]"
						name="solar:home-2-bold"
					/>
					Home
				</a>
				<a
					aria-current={Astro.url.pathname === "/" ? "page" : false}
					class="flex gap-x-1 h-8 items-center justify-center rounded-lg underline-offset-2 hover:underline"
					href="/"
				>	
					<Icon
						class="size-4 drop-shadow-[0px_1.5px_1.5px_rgba(0,0,0,0.175)]"
						name="solar:archive-bold"
					/>
					Blog
				</a>
				-->

				<!-- Ссылки меню -->
				{
					menuLinks.map((link) => (
						<a
							aria-current={Astro.url.pathname === link.path ? "page" : false}
							class="underline-offset-4 transition-all duration-200 ease-out hover:text-accent-one hover:underline decoration-accent-one/30"
							data-astro-prefetch
							href={link.path}
						>
							{link.title}
						</a>
					))
				}
				
				<a
    class="editorial-btn-ghost flex h-8 items-center px-4 text-accent-base"
    data-astro-prefetch
    href="/contact"  // Changed from "src/pages/contact.astro"
>
    Contact
</a>
				
				<!-- Dropdown menu button for large screens. Needs improvement. -->
				<!--
				<button
					aria-expanded="false"
					aria-haspopup="menu"
					aria-label="Open main menu"
					class="hidden md:flex group text-sm relative h-8 w-16 font-medium items-center justify-center px-4 rounded-lg bg-accent-base/5 text-accent-base hover:bg-accent-base/10"
					id="toggle-navigation-menu"
					type="button"
				>
					Menu
				</button>
				-->
			</nav>
			
			<!-- Dropdown menu for large screens. Needs improvement. -->
			<!--
			<div id="menu" class="absolute left-0 right-0 w-fit ml-auto top-16 z-10 hidden" aria-hidden="true">
				<div
					id="menu-body"
					class="fixed bg-bgColor rounded-lg -ml-56 w-56"
				>
					<nav
						aria-label="Main menu"
						class="px-4 py-4 rounded-lg border border-special-lighter bg-special-light shadow-[0px_10px_25px_rgba(0,0,0,0.15)] text-sm flex flex-col gap-y-2 font-medium"
						id="main-navigation-menu"
					>
						{
							menuLinks.map((link) => (
								<a
									aria-current={Astro.url.pathname === link.path ? "page" : false}
									class="text-accent-two underline-offset-2 hover:underline rounded-lg h-8 gap-x-1 px-2 flex justify-center items-center"
									data-astro-prefetch
									href={link.path}
								>
									{link.title}
								</a>
							))
						}
						<a
							class="flex h-8 items-center justify-center rounded-lg bg-accent-base/5 hover:bg-accent-base/10 px-4 text-accent-base"
							data-astro-prefetch
							href="src/pages/contact.astro"
						>
							Contact
						</a>
					</nav>
				</div>
			</div>
			-->

			<div class="ml-auto w-fit">
				<div id="buttons-panel" class="top-4 md:top-8 -ml-[4.5rem] flex space-x-4 items-center">
				 <a 
                        href="https://constitution.congress.gov/constitution/amendment-2/"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="flex h-8 items-center justify-center rounded-sm border border-transparent transition-all duration-200 ease-out hover:border-color-200 hover:bg-color-75 active:scale-[0.98]"
                        aria-label="Second Amendment - US Constitution"
                    >
                        <img 
                            src="/assets/images/USflag.png"
                            alt="US Flag"
                            class="h-6 w-auto drop-shadow-[0px_1.5px_1.5px_rgba(0,0,0,0.175)]"
                        />
                    </a>
					<ThemeToggle />
				</div>
			</div>	
			
			<mobile-button
				aria-expanded="false"
				aria-haspopup="menu"
				aria-label="Open main menu"
                class="group relative h-8 w-8 rounded-sm border border-color-200 bg-color-75 text-accent-base transition-all duration-200 ease-out hover:border-accent-one/30 hover:bg-color-100 active:scale-[0.98] md:invisible md:hidden"
				id="toggle-nav-menu-mobile"
				type="button"
			>
				<Icon
					id="open-nav-menu-icon"
					aria-hidden="true"
					class="absolute start-1/2 top-1/2 h-6 w-6 -translate-x-1/2 -translate-y-1/2 scale-100 opacity-100 transition-all"
					focusable="false"
					name="hugeicons:menu-01"
				/>
				<Icon
					id="close-nav-menu-icon"
					aria-hidden="true"
					class="absolute start-1/2 top-1/2 h-6 w-6 -translate-x-1/2 -translate-y-1/2 scale-0 opacity-0 transition-all"
					focusable="false"
					name="hugeicons:cancel-01"
				/>
			</mobile-button>
		</div>
	</div>
</header>

<div id="drawer" class="fixed inset-0 z-10 hidden" aria-hidden="true">
	<div
		id="drawer-body"
		class="absolute inset-0 -translate-y-full transform border-t border-color-150 bg-bgColor transition-all duration-300 ease-out"
	>
		<!-- Background 
			TODO: This approach is not optimal and requires improvements. 
  			- Too many absolutely positioned elements can affect performance. 
  		-->		
		<div class="pointer-events-none fixed left-1/2 top-0 -ml-[50vw] min-h-screen w-screen overflow-x-hidden overflow-y-visible blur-3xl" aria-hidden="true">
				<div class="editorial-gradient-orb absolute top-[-85%] right-[15%] h-full w-[65%] bg-gradient-to-b from-amber-200/60 via-stone-300/25 to-transparent"></div>
				<div class="editorial-gradient-orb absolute top-[-80%] left-[10%] h-full w-[50%] bg-gradient-to-b from-stone-400/35 to-transparent"></div>
		</div>	
		
		<nav
			aria-label="Mobile navigation menu"
			class="absolute inset-0 flex flex-col items-center justify-center gap-y-5 text-center font-display text-xl font-medium tracking-wide text-accent-base"
			id="nav-menu-mobile"
		>
			<!-- Ссылки меню -->
			{
				menuLinks.map((link) => (
					<a
						aria-current={Astro.url.pathname === link.path ? "page" : false}
						class="underline-offset-4 transition-all duration-200 ease-out hover:text-accent-one hover:underline decoration-accent-one/30"
						data-astro-prefetch
						href={link.path}
					>
						{link.title}
					</a>
				))
			}
			<a
				class="editorial-btn-ghost flex h-8 items-center px-4 text-accent-base"
				data-astro-prefetch
				href="/contact"
			>
				Contact
			</a>
		</nav>
	</div>
</div>

<script>
	document.addEventListener("DOMContentLoaded", () => {
		const toggleNavMenuMobileButton = document.getElementById("toggle-nav-menu-mobile");
		const openMenuIcon = document.getElementById("open-nav-menu-icon");
		const closeMenuIcon = document.getElementById("close-nav-menu-icon");
		const drawer = document.getElementById("drawer");

		// Проверяем существование элементов
		if (!toggleNavMenuMobileButton || !openMenuIcon || !closeMenuIcon || !drawer) {
			console.error("One or more required elements are missing in the DOM.");
			return;
		}

		const drawerBody = document.getElementById("drawer-body");
		if (!drawerBody) {
			console.error("Drawer body element is missing in the DOM.");
			return;
		}

		toggleNavMenuMobileButton.addEventListener("click", () => {
			const isOpen = toggleNavMenuMobileButton.getAttribute("aria-expanded") === "true";

			if (isOpen) {
				// Закрываем меню
				drawerBody.classList.add("opacity-0", "-translate-y-full");
				drawerBody.classList.remove("translate-y-0");

				// Убираем после анимации
				setTimeout(() => {
					drawer.classList.add("hidden");
				}, 300);

				// Меняем иконки
				openMenuIcon.classList.add("scale-100", "opacity-100");
				closeMenuIcon.classList.add("scale-0", "opacity-0");
				closeMenuIcon.classList.remove("scale-100", "opacity-100");
			} else {
				// Показываем меню
				drawer.classList.remove("hidden");
				drawerBody.classList.add("translate-y-0");
				drawerBody.classList.remove("opacity-0", "-translate-y-full");

				// Меняем иконки
				openMenuIcon.classList.add("scale-0", "opacity-0");
				closeMenuIcon.classList.add("scale-100", "opacity-100");
				openMenuIcon.classList.remove("scale-100", "opacity-100");
			}

			// Обновляем состояние кнопки
			toggleNavMenuMobileButton.setAttribute("aria-expanded", (!isOpen).toString());
		});
	});
</script>



